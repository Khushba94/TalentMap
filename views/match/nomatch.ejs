<%- include('../layouts/header', { user: user }) %>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

<main class="container py-5 text-center">
  <h2 class="mb-3"><i class="fas fa-circle-xmark text-danger me-2"></i>No Matches Found</h2>

  <p class="lead">
    We couldn’t find any suitable job matches for 
    <strong><%= resume.candidate_name || ('Resume #' + resume.id) %></strong> at this time.
  </p>

  <p class="text-muted mb-4">
    The resume has been added to the waitlist for future opportunities.
  </p>

  <a class="btn btn-primary" href="/jobs/list">
    <i class="fas fa-briefcase me-2"></i> View Available Jobs
  </a>
</main>

<%- include('../layouts/footer') %>

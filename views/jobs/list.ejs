<%- include('../layouts/header', { user: user }) %>

<div class="container">
  <h2>Job Listings</h2>

  <% if (!jobs.length) { %>
    <p>No jobs available yet.</p>
  <% } else { %>
    <ul>
      <% jobs.forEach(j => { %>
        <li>
          <strong><a href="/jobs/details/<%= j.id %>"><%= j.title %></a></strong>
          — <%= j.location %>
        </li>
      <% }) %>
    </ul>
  <% } %>
</div>

</main>
</body>
</html>